openai>=1.40.0
flask>=3.0.0
numpy>=1.24.0
tiktoken>=0.7.0
pydantic>=2.0.0
python-dotenv>=1.0.0
tenacity>=8.2.0
certifi>=2024.0.0

# Optional: dashboard system metrics
psutil>=5.9.0

# Optional: local backend (Transformers)
transformers>=4.38.0
torch>=2.1.0

# Optional: local backend (vLLM, typically CUDA only)
vllm>=0.4.0; sys_platform != 'darwin'

# Optional: tests
pytest>=8.0.0
